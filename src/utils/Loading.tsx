// return an animation 
// split screen and let user into site
// force to run on first open other than that only run when loading is required

const Loading = () => {
  return ; 
}

export default Loading;